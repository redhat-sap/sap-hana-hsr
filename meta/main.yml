galaxy_info:
  author: Markus Koch, Thomas Bludau
  description: configures SAP HANA SR on a properly deployed HANA on RHEL servers
  license: Apache 2.0
  min_ansible_version: 2.2
  platforms:
  - name: EL
    versions: [ 6, 7 ]

  galaxy_tags: [ 'system', 'sap', 'hana', 'beta' ]

dependencies:
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
        - mk-ansible-roles.saphana-preconfigure
        - mk-ansible-roles.saphana-deploy
